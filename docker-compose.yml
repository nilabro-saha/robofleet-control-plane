version: "3.9"

# Intent:
# Provide a minimal, disposable Kafka-compatible broker for local MVP runs.
# Redpanda is used here to reduce operational overhead versus a full Kafka +
# ZooKeeper setup while preserving the same producer/consumer semantics.

services:
  redpanda:
    image: redpandadata/redpanda:v24.1.9
    container_name: robofleet-redpanda
    command:
      - redpanda
      - start
      - --overprovisioned
      - --smp=1
      - --memory=1G
      - --reserve-memory=0M
      - --check=false
      - --node-id=0
      - --kafka-addr=PLAINTEXT://0.0.0.0:9092
      - --advertise-kafka-addr=PLAINTEXT://localhost:9092
    ports:
      - "9092:9092"
      - "9644:9644"
