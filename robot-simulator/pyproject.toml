[project]
name = "robot-simulator"
version = "0.1.0"
description = "RoboFleet robot telemetry simulator"
requires-python = ">=3.10"
dependencies = [
    "kafka-python==2.0.2"
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
